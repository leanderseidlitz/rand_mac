[Unit]
Description=regenerate the MAC address of interface %I


[Service]
SyslogIdentifier=rand_mac
Type=simple
User=root
Group=root
ExecStart=/usr/local/bin/rand_mac -i "%i"
Restart=no

[Install]
WantedBy=multi-user.target
